module views.generic.errors;

/// (carter):
/// I home-host a translation model that is very quick.
/// I plan on translating all library errors to a number of languages.
/// Until that happens, the separation of errors doesn't make much sense. Hence this explanation.

class ViewErrors {
    static const NO_ALLOWED_METHODS = "No allowed methods provided for view at path: %s, as such no requests will be allowed.";
}